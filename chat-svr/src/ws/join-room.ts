/**
 * ルーム参加メソッドのモジュール。
 * @module ./ws/join-room
 */

/**
 * ルーム参加メソッドクラス。
 */
module.exports = class JoinRoom {
	/**
	 * ルームに参加する。
	 * @param params 参加情報。
	 * @return ルーム情報。
	 */
	invoke(params: { roomNo: number }) {
		// TODO: 未実装
		return [];
	}
}
